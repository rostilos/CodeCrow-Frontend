export const APP_CONFIG = {
    ENDPOINTS: {
        CODE_HOSTING: {
            BITBUCKET_CLOUD: {
                CONFIGURE_CONNECTION: '/dashboard/hosting/configure'
            }
        }
    }
}